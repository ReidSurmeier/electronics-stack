# PiAudioOLED

Auto-generated by `reverse-engineer/compile.js`. Open `PiAudioOLED.kicad_pro` in KiCad 9.

Rev: A

## Blocks

| Ref | Name | Type | Description |
| --- | ---- | ---- | ----------- |
| J1 | u_pi | `pi_zero_2w_header` | Pi zero2w 40-pin header |
| U1 | u_codec | `audio_codec_wm8960_hat` | WM8960 codec (slave, spk amp) |
| U2 | u_oled | `i2c_oled_ssd1306` | SSD1306 OLED 128x64 I2C @0x3C |
| C1 | u_dec | `decap_caps` | Decap 3V3: 4x100nF + 10uF |

## Nets

- **P3V3**: u_pi.3V3, u_codec.VCC, u_codec.DVCC, u_oled.VCC, u_dec.VCC
- **GND**: u_pi.GND, u_codec.GND, u_oled.GND, u_dec.GND
- **I2C_SDA**: u_pi.SDA, u_codec.SDA, u_oled.SDA
- **I2C_SCL**: u_pi.SCL, u_codec.SCL, u_oled.SCL
- **I2S_BCLK**: u_pi.I2S_BCLK, u_codec.BCLK
- **I2S_LRCLK**: u_pi.I2S_LRCLK, u_codec.LRCLK
- **I2S_PI_TO_CODEC**: u_pi.I2S_DOUT, u_codec.DIN
- **I2S_CODEC_TO_PI**: u_pi.I2S_DIN, u_codec.DOUT

## Notes

Pi 3V3 rail is limited (~50mA) — verify codec + OLED + bulk current draw.
If load exceeds ~40mA, add an external regulator (use buck_3v3 block) on
the Pi 5V rail and feed P3V3 from there instead.
Speakers connect directly to u_codec SPK_LP/LN/RP/RN pins (not modeled here).

